name: Publish

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (defaults to v<package.json version>)"
        required: false
        type: string
      dry-run:
        description: "Build + pack + release, but skip npm publish"
        required: false
        type: boolean
        default: false

jobs:
  # Reuse the parallelized build pipeline. It produces the runtime wasm,
  # host wasm, every grammar, runs the test suite, and uploads the packed
  # `.tgz` as an artifact this workflow consumes below — no rebuild here.
  build:
    uses: ./.github/workflows/build.yml

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      - name: Download tarball
        uses: actions/download-artifact@v8
        with:
          name: ${{ needs.build.outputs.tarball-artifact }}
          path: .

      # The per-platform native addons built by build.yml's package-node matrix.
      # Each artifact preserves its npm/<platform>/ path, but the .node basenames
      # already carry the platform (arborium-rt-node.<platformArchABI>.node), so
      # we flatten them all into ./node-addons/ for flat release attachments.
      # We attach only the raw .node binaries — not the full npm package.
      - name: Download Node addons
        uses: actions/download-artifact@v8
        with:
          pattern: node-addon-*
          path: node-addons-raw
          merge-multiple: true

      - name: Flatten Node addons
        run: |
          mkdir -p node-addons
          find node-addons-raw -name '*.node' -exec mv {} node-addons/ \;
          ls -l node-addons

      - name: Resolve tag
        id: meta
        run: |
          tag="${{ inputs.tag }}"
          if [ -z "$tag" ]; then
            tag="v${{ needs.build.outputs.version }}"
          fi
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"

      # Publishing is allowed to fail (network, version already published,
      # token trouble) — we still want the GitHub release + tarball upload
      # below to happen so the artifact is recoverable.
      - name: Publish to npm
        id: publish
        if: ${{ !inputs.dry-run }}
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish "${{ needs.build.outputs.tarball-name }}" --access public

      - name: Create GitHub release
        if: ${{ always() }}
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          tag="${{ steps.meta.outputs.tag }}"
          notes="Release ${tag}"
          if [ "${{ steps.publish.outcome }}" = "failure" ]; then
            notes="${notes}

          > npm publish failed in this run — install from the attached tarball."
          elif [ "${{ inputs.dry-run }}" = "true" ]; then
            notes="${notes}

          > dry-run: npm publish was skipped."
          fi
          gh release create "$tag" \
            "${{ needs.build.outputs.tarball-name }}" \
            node-addons/*.node \
            --target "$GITHUB_SHA" \
            --title "$tag" \
            --notes "$notes"
