# Maven coordinates + version for the published AAR. This is the Android
# target's own version line, independent of the wasm/node package versions.
GROUP=com.discord
ARTIFACT_ID=arborium-rt-android
VERSION_NAME=0.1.0

android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
kotlin.code.style=official
